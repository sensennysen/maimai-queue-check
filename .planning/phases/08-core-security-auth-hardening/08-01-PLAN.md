# Phase 08: Core Security & Auth Hardening - Plan

**Wave:** 1
**Autonomous:** true
**Requirements:** [AUTH-03], [SEC-07]

## Goal
Verify and finalize the hardening of session caching and the score import boundary. Ensure roles strictly reside in-memory and that edge function security is validated.

## Tasks

### [Auth] Verify In-Memory Role Sequestration <!-- id: 0 -->
- [ ] Audit `src/contexts/AuthContext.jsx` and `src/services/supabase/roles.js` (if exists) for any unauthorized persistence.
    - `<action>`: Perform a thorough grep across the entire `src` directory for any `localStorage` or `sessionStorage` keys that look like `user_role`, `permissions`, or `role_cache`. Remove any dead code found.
    - `<read_first>`: [src/contexts/AuthContext.jsx](file:///e:/git/smf-queue-check/src/contexts/AuthContext.jsx)
    - `<acceptance_criteria>`: `grep -r "localStorage" src` and `grep -r "sessionStorage" src` do not return any hits related to user roles or permissions.
    - `<verification>`: Run the grep commands and confirm no matches.

### [Security] Validate Score Import RLS <!-- id: 1 -->
- [ ] Verify `import_sessions` table RLS policies via SQL check.
    - `<action>`: Run `npx supabase sql query "select * from pg_policies where tablename = 'import_sessions';"` (or equivalent) to verify that `auth.uid() = user_id` is enforced for all relevant operations (INSERT, SELECT, UPDATE).
    - `<read_first>`: [supabase/migrations/](file:///e:/git/smf-queue-check/supabase/migrations/) (if available)
    - `<acceptance_criteria>`: Output shows policy `Enable insert/select for users based on user_id` exists and is correctly defined.
    - `<verification>`: SQL query output matches the required policy structure.

## Verification

### Automated
- [ ] `npm run lint` passes.
- [ ] `npm run test` passes (specifically any auth or rls smoke tests).

### Manual
- [ ] Log in, open DevTools -> Application -> Local Storage. Confirm ONLY Supabase auth tokens exist (e.g., `sb-[project-id]-auth-token`).
- [ ] Confirm no `user_roles` key exists in Local Storage or Session Storage.
- [ ] Verify "Best 50" export (which may use import sessions) still functions correctly under the current RLS.
