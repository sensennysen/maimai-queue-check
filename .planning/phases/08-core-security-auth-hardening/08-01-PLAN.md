# Phase 08: Core Security & Auth Hardening - Plan

**Wave:** 1
**Autonomous:** true
**Requirements:** [AUTH-03], [SEC-07]

## Goal
Secure the session caching layer and score import boundary against XSS and unauthorized access by removing long-lived role caches and enforcing strict RLS.

## Tasks

### [Auth] Remove Role Caching
- [ ] Remove `getCachedRoles` and `cacheRoles` from `AuthContext.jsx` <!-- id: 0 -->
    - `<action>`: Remove the functions and their calls in `useEffect`, `onAuthStateChange`, and `signOut`.
    - `<read_first>`: [AuthContext.jsx](file:///d:/Dev%20Projects/maimai-queue-check/src/contexts/AuthContext.jsx)
    - `<acceptance_criteria>`: `AuthContext.jsx` does not contain `localStorage.getItem('user_roles_')`.

## Verification

### Automated
- [ ] `npm run lint` passes.
- [ ] No role-related `localStorage` calls in `AuthContext.jsx`.

### Manual
- [ ] Log in, verify session exists but no role cache in `localStorage`.
- [ ] Verify UI still reflects correct roles by checking "Admin" or "Super Admin" badges if applicable.
- [ ] Verify bookmarklet still functions by initiating an import (if test environment permits).
