---
wave: 1
depends_on: []
files_modified:
  - src/features/songs/components/SongDatabase.jsx
  - src/features/songs/components/SongSelectionModal.jsx
  - src/features/songs/components/SongDatabase.module.css
  - src/features/songs/components/SongSelectionModal.module.css
autonomous: true
---

# Plan 07-01: SEC-05 CSP Tightening

Refactor component-injected `<style>` tags to CSS modules to support stricter Content Security Policy by removing `'unsafe-inline'`.

## Tasks

### 1. Create CSS Modules
<read_first>
- src/features/songs/components/SongDatabase.jsx
- src/features/songs/components/SongSelectionModal.jsx
</read_first>
<action>
Create `src/features/songs/components/SongDatabase.module.css` and `src/features/songs/components/SongSelectionModal.module.css` with the media queries currently injected via `dangerouslySetInnerHTML`.
</action>
<acceptance_criteria>
- File `src/features/songs/components/SongDatabase.module.css` exists.
- File `src/features/songs/components/SongSelectionModal.module.css` exists.
- Both contain `@media (min-width: 992px)` rules with `grid-template-columns`.
</acceptance_criteria>

### 2. Refactor SongDatabase
<read_first>
- src/features/songs/components/SongDatabase.jsx
- src/features/songs/components/SongDatabase.module.css
</read_first>
<action>
Modify `SongDatabase.jsx`:
- Import styles from `./SongDatabase.module.css`.
- Remove the `<style dangerouslySetInnerHTML={{ __html: ... }} />` block.
- Apply the appropriate class from the CSS module to the `.song-db-grid` container.
</action>
<acceptance_criteria>
- `SongDatabase.jsx` does not contain `dangerouslySetInnerHTML`.
- `SongDatabase.jsx` imports `styles` from `.module.css`.
- Grid layout still works on desktop and mobile.
</acceptance_criteria>

### 3. Refactor SongSelectionModal
<read_first>
- src/features/songs/components/SongSelectionModal.jsx
- src/features/songs/components/SongSelectionModal.module.css
</read_first>
<action>
Modify `SongSelectionModal.jsx`:
- Import styles from `./SongSelectionModal.module.css`.
- Remove the `<style dangerouslySetInnerHTML={{ __html: ... }} />` block.
- Apply the appropriate class from the CSS module to the `.song-modal-grid` container.
</action>
<acceptance_criteria>
- `SongSelectionModal.jsx` does not contain `dangerouslySetInnerHTML`.
- `SongSelectionModal.jsx` imports `styles` from `.module.css`.
- Grid layout still works on desktop and mobile.
</acceptance_criteria>

## Verification

### Automated
- `npm run lint` pass.

### must_haves
- No more `<style>` tag injection in SongDatabase or SongSelectionModal.
- Layout remains responsive (2-row/1-row switch at 992px).
