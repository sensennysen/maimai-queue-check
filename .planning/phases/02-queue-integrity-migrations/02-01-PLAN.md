---
phase: 02-queue-integrity-migrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/services/supabase/queue.js]
autonomous: true
requirements: [QUEUE-01]
must_haves:
  truths:
    - "finishGame transitions an entry to COMPLETED and starts the next WAITING entry as PLAYING atomically"
  artifacts:
    - path: "src/services/supabase/queue.js"
      provides: "Queue state transition logic"
      contains: "finishGame"
  key_links:
    - from: "src/services/supabase/queue.js"
      to: "supabase"
      via: "update and delete/insert"
---

<objective>
Refactor `finishGame` in `queueService` to handle atomic transitions and move towards server-side guarantees. 

While we don't have a direct Postgres migration system in this repo yet, we'll improve the client-side logic in `queueService` to be more robust, and prepare placeholders for RPC calls if applicable.

Purpose: Ensure transition from 'playing' to 'completed' and 'waiting' to 'playing' is as robust as possible.
Output: Refactored `finishGame` method.
</objective>

<execution_context>
@C:/Users/games/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/services/supabase/queue.js
@src/constants/queue.js
@src/constants/database.js

<interfaces>
From src/services/supabase/queue.js:
```javascript
export const queueService = {
  async finishGame(currentPlayingId, nextWaitingId) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for finishGame atomicity</name>
  <files>src/__tests__/queue-transitions.test.jsx</files>
  <behavior>
    - Test: finishGame(playingId, waitingId) should call update for both IDs.
    - Test: If playingId is null, only nextWaitingId should be started.
    - Test: If nextWaitingId is null, only currentPlayingId should be ended.
  </behavior>
  <action>
    Create a new test file `src/__tests__/queue-transitions.test.jsx` that mocks Supabase and tests the `finishGame` method of `queueService`.
  </action>
  <verify>
    <automated>npm test src/__tests__/queue-transitions.test.jsx</automated>
  </verify>
  <done>Tests pass with current implementation (as baseline).</done>
</task>

<task type="auto">
  <name>Task 2: Refactor finishGame for robustness</name>
  <files>src/services/supabase/queue.js</files>
  <action>
    Update `finishGame` to handle errors more gracefully and ensure sequential updates are handled.
    In the future, this should be replaced by a single `rpc` call, but for now we'll ensure both updates fire correctly.
  </action>
  <verify>
    <automated>npm test src/__tests__/queue-transitions.test.jsx</automated>
  </verify>
  <done>finishGame passes all transition tests.</done>
</task>

</tasks>

<verification>
Run `npm test src/__tests__/queue-transitions.test.jsx` to ensure all transitions work as expected.
</verification>

<success_criteria>
- `finishGame` refactored to be more robust.
- Regression tests covering all status transition scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/02-queue-integrity-migrations/02-01-SUMMARY.md`
</output>
