---
phase: 02-queue-integrity-migrations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/services/supabase/queue.js]
autonomous: true
requirements: [QUEUE-02]
must_haves:
  truths:
    - "updateOrderPositions can handle arbitrary swap updates"
  artifacts:
    - path: "src/services/supabase/queue.js"
      provides: "Bulk update mechanics"
      contains: "updateOrderPositions"
    - path: "src/hooks/useQueueActions.js"
      provides: "UI layer call for reordering players"
  key_links:
    - from: "src/hooks/useQueueActions.js"
      to: "src/services/supabase/queue.js"
      via: "updateOrderPositions"
---

<objective>
Refactor `updateOrderPositions` in `queueService` to move from sequential `update` calls to a cleaner `upsert` or single-request pattern.

Purpose: Reduce network overhead and potential race conditions for bulk updates.
Output: Refactored `updateOrderPositions` method.
</objective>

<execution_context>
@C:/Users/games/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/services/supabase/queue.js
@src/hooks/useQueueActions.js

<interfaces>
From src/services/supabase/queue.js:
```javascript
export const queueService = {
  async updateOrderPositions(updates) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for updateOrderPositions</name>
  <files>src/__tests__/queue-order.test.jsx</files>
  <behavior>
    - Test: updateOrderPositions(updates) should update all elements correctly.
    - Test: Should handle multi-element swaps.
  </behavior>
  <action>
    Create a new test file `src/__tests__/queue-order.test.jsx` that mocks Supabase and tests the `updateOrderPositions` method.
  </action>
  <verify>
    <automated>npm test src/__tests__/queue-order.test.jsx</automated>
  </verify>
  <done>Tests pass with baseline sequential update.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor updateOrderPositions to use single upsert if possible</name>
  <files>src/services/supabase/queue.js</files>
  <action>
    While we're on client-side, using `supabase.from(...).upsert(updates)` would be more efficient than looping updates.
    Ensure we pass the correct keys so we only update `id` and `order_position`.
  </action>
  <verify>
    <automated>npm test src/__tests__/queue-order.test.jsx</automated>
  </verify>
  <done>updateOrderPositions uses single efficient update call.</done>
</task>

</tasks>

<verification>
Run `npm test src/__tests__/queue-order.test.jsx`.
</verification>

<success_criteria>
- Reordering operations reduced to single request where possible.
- Bulk reordering tested with regression suite.
</success_criteria>

<output>
After completion, create `.planning/phases/02-queue-integrity-migrations/02-02-SUMMARY.md`
</output>
