---
phase: 02-queue-integrity-migrations
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: []
autonomous: true
requirements: [QUEUE-01, QUEUE-02]
must_haves:
  truths:
    - "'at most one' status PLAYING for any cabinet."
    - "no gaps in order_position sequence."
  artifacts:
    - path: "src/__tests__/queue-integrity.test.jsx"
      provides: "Full suite for queue invariants"
---

<objective>
Finalize tests for queue invariants across all operations, ensuring the migrations to more robust service-layer methods didn't introduce functional leaks. 

Focus: Specifically ensure "at most one PLAYING" per cabinet.
Purpose: Secure the "Integrity" part of the phase goal.
Output: Expanded integration tests suite.
</objective>

<execution_context>
@C:/Users/games/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/services/supabase/queue.js
@src/hooks/useQueueActions.js

<interfaces>
From src/services/supabase/queue.js:
- `getQueueEntries`
- `addQueueEntry`
- `finishGame`
- `updateOrderPositions`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a unified integrity test suite</name>
  <files>src/__tests__/queue-integrity.test.jsx</files>
  <behavior>
    - Test: adding a second player when one is already playing should result in 'waiting' for the second one.
    - Test: finishing a game should transition the first 'waiting' to 'playing' and the original 'playing' to 'completed'.
    - Test: swappings wait positions between waiting entries should correctly reorder them.
  </behavior>
  <action>
    Create a comprehensive test file `src/__tests__/queue-integrity.test.jsx` covering the overall state machines.
  </action>
  <verify>
    <automated>npm test src/__tests__/queue-integrity.test.jsx</automated>
  </verify>
  <done>Integrity tests pass against latest implementation.</done>
</task>

</tasks>

<verification>
Run `npm test src/__tests__/queue-integrity.test.jsx`.
</verification>

<success_criteria>
- Clear evidence that only 1 player can be PLAYING at any time.
- All ordering invariants preserved through swaps.
</success_criteria>

<output>
After completion, create `.planning/phases/02-queue-integrity-migrations/02-03-SUMMARY.md`
</output>
